# Cross-Project Lessons (5/69 most relevant)

Auto-injected by `inject_lessons_interactive.py` at SessionStart.
Filtered by expertise tags, recent file activity, and task keywords.

<!-- LESSON_ACTIVE: mark-phase-skipped -->
**Mark Phase Skipped:** The agent frequently completes work and commits but forgets to set
"passes": true in plan.md. This causes the next iteration to re-attempt
the same task, wasting cycles.

Observed in: AngryBirds (1x), DissonantDreams-Unity (1x), EventDesignedLearning (5x), campaign-finance-network (60x), comfyprompts (5x), drawing-agent (6x), flavor-network (61x), gym-mouse (1x), mod-my-week-platform (104x), murder-mystery (7x), prediction_betting (4x), ralph-universal (49x), unity-py-sim (16x)
  Fix: 1. PROMPT.md Phase 6 (Mark) must be bold with explicit "CRITICAL" warning
2. Manager review checks if the diff includes a passes:true change in plan.md
3. If the last commit message references a task but plan.md wasn't updated, flag it

<!-- LESSON_ACTIVE: template-task-infinite-loop -->
**Template Task Infinite Loop:** When plan.md includes a JSON task block as a "format example" (template),
prepare_context.py parses it as a real pending task. The agent picks it up
every iteration but can't complete it (it's not a real task), causing an
infinite loop of wasted iterations.

Observed in: comfyprompts — 8 iterations stuck on template task.
  Fix: 1. Template/example task blocks must be inside HTML comments (<!-- -->)
   so the JSON parser doesn't see them
2. OR the template must have "passes": true so it's filtered out
3. prepare_context.py should warn if a task has no meaningful description

<!-- LESSON_ACTIVE: ui-projects-need-test-infrastructure -->
**Ui Projects Need Test Infrastructure:** JavaScript/React projects consistently lack test infrastructure. 84% of
flavor-network iterations had no test coverage. mod-my-week-platform had 0%
test involvement and was the ONLY project with gate failures (3 failures on
audit/refactoring tasks).
Observed in: flavor-network (84% test gap), mod-my-week-platform (0% tests, 3 gate failures), EventDesignedLearning (inconsistent)
  Fix: 1. Bootstrap test infrastructure (jest, react-testing-library) in project setup phase
2. Add test-writing as first-class task category in plan.md
3. Gate should warn (not block) when source files change without corresponding test files
4. Template test suites for common patterns (component rendering, API calls, state management)

<!-- LESSON_ACTIVE: repetition-drawing-agent-run-the-15-prompt-mvp-eval-end-to-e -->
**Repetition Drawing Agent Run The 15 Prompt Mvp Eval End To E:** Task repeated 5 times without progress in drawing-agent: Run the 15-prompt MVP eval end-to-end live: 15 HF generations + 15 LLM-judge sco

Stuck task: Run the 15-prompt MVP eval end-to-end live: 15 HF generations + 15 LLM-judge scorings + user scores at least 3 items (15
Observed in: drawing-agent
  Fix: 1. After 2 consecutive iterations on the same task, inject a warning into the agent prompt
2. Consider decomposing the task into smaller subtasks in plan.md
3. If a task fails 3+ times, escalate to human review before retrying

<!-- LESSON_ACTIVE: test-coverage-app-jsx -->
**Test Coverage App Jsx:** Test coverage gaps detected: 7436 source changes without corresponding tests

- App.jsx: 67 times (EventDesignedLearning, campaign-finance-network, flavor-network, mod-my-week-platform)
- TeamCodeWorkspace.jsx: 28 times (mod-my-week-platform)
- fix-enemy-ai-seeking.js: 25 times (mod-my-week-platform)
- fix-tongue-tickled-rotation.js: 25 times (mod-my-week-platform)
- inspect-tongue-tickled.js: 25 times (mod-my-week-platform)
Observed in: AngryBirds, DissonantDreams-Unity, EventDesignedLearning, FSM_Command_Patterns, campaign-finance-network, comfyprompts, drawing-agent, flavor-network, gym-mouse, mod-my-week-platform, murder-mystery, prediction_betting, ralph-universal, unity-py-sim
  Fix: 1. Plan mode should auto-discover test gaps (grep for modules without test_ counterparts)
2. Test-writing should be a first-class task category in every project's plan.md
3. The manager review should flag changes to untested modules as NEEDS_FIX

