# Active Lessons (5/149 most relevant) — read + extend

These lessons were scored against your expertise tags, recent file
activity, and pending task keywords; the most relevant are surfaced
here. **Read them and apply when relevant.** When you hit a failure
no lesson covers, *extend the corpus*: copy `.claude/.ralph-lesson-template.md`
to `lessons/<stem>.md`, fill it in, and tag the commit with the new stem.
See `docs/bridge-protocol.md` §4h for the full harvest protocol.

<!-- This file is regenerated by inject_lessons_interactive.py at every
     SessionStart, so direct edits here are lost — extend at lessons/<stem>.md instead. -->

<!-- LESSON_ACTIVE: file-failure-app-smoke-test-tsx-bookstrapcb -->
**File Failure App Smoke Test Tsx Bookstrapcb:** app.smoke.test.tsx has a 60% gate failure rate (3.0/5.0 iterations)

Failing gate checks: `manager_review_unknown` (3x), `tests` (2x), `denylist` (1x).

Blind-TDD phase at failure: `error` (2x).

Frequently co-changed files: `postcss.config.js`, `main.tsx`, `playwright.config.ts`, `firebase.ts`, `tailwind.config.ts`.

Recent failing commits:
- 2026-04-26 3f97f4b (bookstrapCB) — checks: tests, manager_review_unknown
- 2026-04-26 3f97f4b (bookstrapCB) — checks: tests, manager_review_unknown
- 2026-04-26 3f97f4b (bookstrapCB) — checks: denylist, manager_review_unknown

Observed in: bookstrapCB
  Fix: 1. Manager review keeps rejecting changes here. Read the most recent review verdicts from the dashboard and address the cited concerns before retrying.
2. Changes here usually touch `postcss.config.js`, `main.tsx`, `playwright.config.ts`. Either inspect those files for shared state, or split the commit so a single boundary fails per gate run.
3. After applying any of the above, tag the commit message with `<lesson_applied stem='file-failure-app-smoke-test-tsx-bookstrapcb' how='N' />` where N is the step you used. This is how the corpus learns whether the lesson is actually working (see docs/bridge-protocol.md §4h).

<!-- LESSON_ACTIVE: ui-projects-need-test-infrastructure -->
**Ui Projects Need Test Infrastructure:** JavaScript/React projects consistently lack test infrastructure. 84% of
flavor-network iterations had no test coverage. mod-my-week-platform had 0%
test involvement and was the ONLY project with gate failures (3 failures on
audit/refactoring tasks).
Observed in: flavor-network (84% test gap), mod-my-week-platform (0% tests, 3 gate failures), EventDesignedLearning (inconsistent)
  Fix: 1. Bootstrap test infrastructure (jest, react-testing-library) in project setup phase
2. Add test-writing as first-class task category in plan.md
3. Gate should warn (not block) when source files change without corresponding test files
4. Template test suites for common patterns (component rendering, API calls, state management)

<!-- LESSON_ACTIVE: file-failure-playwright-config-ts-ka-site -->
**File Failure Playwright Config Ts Ka Site:** playwright.config.ts has a 71% gate failure rate (5.0/7.0 iterations)

Failing gate checks: `manager_review_unknown` (5x), `tests` (3x), `denylist` (1x).

Blind-TDD phase at failure: `error` (2x).

Top error signatures:
- (1x) RUN v4.1.5 KA_Site
- (1x) No test files found, exiting with code 1
- (1x) filter: src/

Frequently co-changed files: `postcss.config.js`, `main.tsx`, `firebase.ts`, `tailwind.config.ts`, `setup.ts`.

Recent failing commits:
- 2026-04-29 (KA_Site) — checks: manager_review_unknown
- 2026-04-29 (KA_Site) — checks: tests, manager_review_unknown | RUN v4.1.5 KA_Site
- 2026-04-26 3f97f4b (bookstrapCB) — checks: tests, manager_review_unknown

Observed in: KA_Site, bookstrapCB
  Fix: 1. Manager review keeps rejecting changes here. Read the most recent review verdicts from the dashboard and address the cited concerns before retrying.
2. Reproduce the dominant error first: `RUN v4.1.5 KA_Site` (seen 1x). If your change doesn't address it, expect the gate to fail again.
3. Changes here usually touch `postcss.config.js`, `main.tsx`, `firebase.ts`. Either inspect those files for shared state, or split the commit so a single boundary fails per gate run.
4. After applying any of the above, tag the commit message with `<lesson_applied stem='file-failure-playwright-config-ts-ka-site' how='N' />` where N is the step you used. This is how the corpus learns whether the lesson is actually working (see docs/bridge-protocol.md §4h).

<!-- LESSON_ACTIVE: file-failure-run-task3-tests-py-unity-py-sim -->
**File Failure Run Task3 Tests Py Unity Py Sim:** run_task3_tests.py has a 89% gate failure rate (8.0/9.0 iterations)

Failing gate checks: `manager_review_unknown` (8x), `tests` (7x), `type_checker` (1x).

Blind-TDD phase at failure: `red` (3x).

Top error signatures:
- (1x) bird.py:*:12 - error: "body_type" is not a known attribute of "None" (reportOptionalMemberAccess)
- (1x) bird.py:*:19 - error: "velocity" is not a known attribute of "None" (reportOptionalMemberAccess)
- (1x) brick.py:*:30 - error: Cannot access attribute "game_object" for class "object"

Frequently co-changed files: `test_pacman_debug.py`, `run_tasks45_tests.py`, `test_ghost_reset.py`, `movement.py`, `ghost_chase.py`.

Recent failing commits:
- 2026-04-26 c7110e0 (unity-py-sim) — checks: type_checker, manager_review_unknown | bird.py:*:12 - error: "body_type" is not a known attribute of "None" (reportOptionalMemberAccess)
- 2026-04-10 7c225eb (unity-py-sim) — checks: tests, manager_review_unknown
- 2026-04-10 7c225eb (unity-py-sim) — checks: tests, manager_review_unknown

Observed in: unity-py-sim
  Fix: 1. Manager review keeps rejecting changes here. Read the most recent review verdicts from the dashboard and address the cited concerns before retrying.
2. Reproduce the dominant error first: `bird.py:*:12 - error: "body_type" is not a known attribute of "None" (reportOptionalMemberAccess)` (seen 1x). If your change doesn't address it, expect the gate to fail again.
3. Changes here usually touch `test_pacman_debug.py`, `run_tasks45_tests.py`, `test_ghost_reset.py`. Either inspect those files for shared state, or split the commit so a single boundary fails per gate run.
4. After applying any of the above, tag the commit message with `<lesson_applied stem='file-failure-run-task3-tests-py-unity-py-sim' how='N' />` where N is the step you used. This is how the corpus learns whether the lesson is actually working (see docs/bridge-protocol.md §4h).

<!-- LESSON_ACTIVE: file-failure-run-tasks45-tests-py-unity-py-sim -->
**File Failure Run Tasks45 Tests Py Unity Py Sim:** run_tasks45_tests.py has a 89% gate failure rate (8.0/9.0 iterations)

Failing gate checks: `manager_review_unknown` (8x), `tests` (7x), `type_checker` (1x).

Blind-TDD phase at failure: `red` (3x).

Top error signatures:
- (1x) bird.py:*:12 - error: "body_type" is not a known attribute of "None" (reportOptionalMemberAccess)
- (1x) bird.py:*:19 - error: "velocity" is not a known attribute of "None" (reportOptionalMemberAccess)
- (1x) brick.py:*:30 - error: Cannot access attribute "game_object" for class "object"

Frequently co-changed files: `test_pacman_debug.py`, `test_ghost_reset.py`, `run_task3_tests.py`, `movement.py`, `ghost_chase.py`.

Recent failing commits:
- 2026-04-26 c7110e0 (unity-py-sim) — checks: type_checker, manager_review_unknown | bird.py:*:12 - error: "body_type" is not a known attribute of "None" (reportOptionalMemberAccess)
- 2026-04-10 7c225eb (unity-py-sim) — checks: tests, manager_review_unknown
- 2026-04-10 7c225eb (unity-py-sim) — checks: tests, manager_review_unknown

Observed in: unity-py-sim
  Fix: 1. Manager review keeps rejecting changes here. Read the most recent review verdicts from the dashboard and address the cited concerns before retrying.
2. Reproduce the dominant error first: `bird.py:*:12 - error: "body_type" is not a known attribute of "None" (reportOptionalMemberAccess)` (seen 1x). If your change doesn't address it, expect the gate to fail again.
3. Changes here usually touch `test_pacman_debug.py`, `test_ghost_reset.py`, `run_task3_tests.py`. Either inspect those files for shared state, or split the commit so a single boundary fails per gate run.
4. After applying any of the above, tag the commit message with `<lesson_applied stem='file-failure-run-tasks45-tests-py-unity-py-sim' how='N' />` where N is the step you used. This is how the corpus learns whether the lesson is actually working (see docs/bridge-protocol.md §4h).

