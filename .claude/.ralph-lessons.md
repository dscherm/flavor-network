# Active Lessons (5/150 most relevant) — read + extend

These lessons were scored against your expertise tags, recent file
activity, and pending task keywords; the most relevant are surfaced
here. **Read them and apply when relevant.** When you hit a failure
no lesson covers, *extend the corpus*: copy `.claude/.ralph-lesson-template.md`
to `lessons/<stem>.md`, fill it in, and tag the commit with the new stem.
See `docs/bridge-protocol.md` §4h for the full harvest protocol.

<!-- This file is regenerated by inject_lessons_interactive.py at every
     SessionStart, so direct edits here are lost — extend at lessons/<stem>.md instead. -->

<!-- LESSON_ACTIVE: file-failure-home-spec-ts-ka-site -->
**File Failure Home Spec Ts Ka Site:** home.spec.ts has a 67% gate failure rate (2.0/3.0 iterations)

Failing gate checks: `manager_review_unknown` (2x), `tests` (1x).

Top error signatures:
- (1x) RUN v4.1.5 KA_Site
- (1x) No test files found, exiting with code 1
- (1x) filter: src/

Frequently co-changed files: `layout.tsx`, `page.tsx`, `blind_tdd_audit.py`, `vitest.setup.ts`, `vitest.config.ts`.

Recent failing commits:
- 2026-04-29 (KA_Site) — checks: manager_review_unknown
- 2026-04-29 (KA_Site) — checks: tests, manager_review_unknown | RUN v4.1.5 KA_Site

Observed in: KA_Site
  Fix: 1. Manager review keeps rejecting changes here. Read the most recent review verdicts from the dashboard and address the cited concerns before retrying.
2. Reproduce the dominant error first: `RUN v4.1.5 KA_Site` (seen 1x). If your change doesn't address it, expect the gate to fail again.
3. Changes here usually touch `layout.tsx`, `page.tsx`, `blind_tdd_audit.py`. Either inspect those files for shared state, or split the commit so a single boundary fails per gate run.
4. After applying any of the above, tag the commit message with `<lesson_applied stem='file-failure-home-spec-ts-ka-site' how='N' />` where N is the step you used. This is how the corpus learns whether the lesson is actually working (see docs/bridge-protocol.md §4h).

<!-- LESSON_ACTIVE: test-coverage-app-jsx -->
**Test Coverage App Jsx:** Test coverage gaps detected: 7654 source changes without corresponding tests

Top offenders:
- `App.jsx`: 67 times (EventDesignedLearning, campaign-finance-network, flavor-network, mod-my-week-platform)
- `TeamCodeWorkspace.jsx`: 28 times (mod-my-week-platform)
- `index.ts`: 25 times (bookstrapCB)
- `fix-enemy-ai-seeking.js`: 25 times (mod-my-week-platform)
- `fix-tongue-tickled-rotation.js`: 25 times (mod-my-week-platform)

Recent uncovered changes to `App.jsx`:
- 2026-04-14 7963b4f (flavor-network)
- 2026-04-10 0fbe191 (mod-my-week-platform)
- 2026-04-09 eb583fc (mod-my-week-platform)

Observed in: AngryBirds, DissonantDreams-Unity, EventDesignedLearning, FSM_Command_Patterns, KA_Site, bookstrapCB, campaign-finance-network, comfyprompts, drawing-agent, flavor-network, gym-mouse, mod-my-week-platform, murder-mystery, prediction_betting, ralph-r5-test, ralph-universal, unity-py-sim, voxbed-q
  Fix: 1. Start with `App.jsx` (67 uncovered changes). Find the call sites with `git grep` and write the smallest test that exercises the most-frequently changed path.
2. When `--blind-tdd` is enabled, prefer adding `acceptance_criteria` to the task in `plan.md` so the next attempt routes through the blind writer.
3. Treat any source change without a test as `NEEDS_FIX` in manager review — this is the cheapest gate to flip.
4. After applying any of the above, tag the commit message with `<lesson_applied stem='test-coverage-app-jsx' how='N' />` where N is the step you used. This is how the corpus learns whether the lesson is actually working (see docs/bridge-protocol.md §4h).

<!-- LESSON_ACTIVE: ui-projects-need-test-infrastructure -->
**Ui Projects Need Test Infrastructure:** JavaScript/React projects consistently lack test infrastructure. 84% of
flavor-network iterations had no test coverage. mod-my-week-platform had 0%
test involvement and was the ONLY project with gate failures (3 failures on
audit/refactoring tasks).
Observed in: flavor-network (84% test gap), mod-my-week-platform (0% tests, 3 gate failures), EventDesignedLearning (inconsistent)
  Fix: 1. Bootstrap test infrastructure (jest, react-testing-library) in project setup phase
2. Add test-writing as first-class task category in plan.md
3. Gate should warn (not block) when source files change without corresponding test files
4. Template test suites for common patterns (component rendering, API calls, state management)

<!-- LESSON_ACTIVE: file-failure-playwright-config-ts-bookstrapcb -->
**File Failure Playwright Config Ts Bookstrapcb:** playwright.config.ts has a 75% gate failure rate (3.0/4.0 iterations)

Failing gate checks: `manager_review_unknown` (3x), `tests` (2x), `denylist` (1x).

Blind-TDD phase at failure: `error` (2x).

Frequently co-changed files: `postcss.config.js`, `main.tsx`, `firebase.ts`, `tailwind.config.ts`, `setup.ts`.

Recent failing commits:
- 2026-04-26 3f97f4b (bookstrapCB) — checks: tests, manager_review_unknown
- 2026-04-26 3f97f4b (bookstrapCB) — checks: tests, manager_review_unknown
- 2026-04-26 3f97f4b (bookstrapCB) — checks: denylist, manager_review_unknown

Observed in: bookstrapCB
  Fix: 1. Manager review keeps rejecting changes here. Read the most recent review verdicts from the dashboard and address the cited concerns before retrying.
2. Changes here usually touch `postcss.config.js`, `main.tsx`, `firebase.ts`. Either inspect those files for shared state, or split the commit so a single boundary fails per gate run.
3. After applying any of the above, tag the commit message with `<lesson_applied stem='file-failure-playwright-config-ts-bookstrapcb' how='N' />` where N is the step you used. This is how the corpus learns whether the lesson is actually working (see docs/bridge-protocol.md §4h).

<!-- LESSON_ACTIVE: file-failure-vitest-config-ts-ka-site -->
**File Failure Vitest Config Ts Ka Site:** vitest.config.ts has a 60% gate failure rate (3.0/5.0 iterations)

Failing gate checks: `manager_review_unknown` (3x), `tests` (1x).

Top error signatures:
- (1x) RUN v4.1.5 KA_Site
- (1x) No test files found, exiting with code 1
- (1x) filter: src/

Frequently co-changed files: `page.tsx`, `layout.tsx`, `blind_tdd_audit.py`, `home.spec.ts`, `vitest.setup.ts`.

Recent failing commits:
- 2026-04-30 9ac7df6 (KA_Site) — checks: manager_review_unknown
- 2026-04-29 (KA_Site) — checks: manager_review_unknown
- 2026-04-29 (KA_Site) — checks: tests, manager_review_unknown | RUN v4.1.5 KA_Site

Observed in: KA_Site
  Fix: 1. Manager review keeps rejecting changes here. Read the most recent review verdicts from the dashboard and address the cited concerns before retrying.
2. Reproduce the dominant error first: `RUN v4.1.5 KA_Site` (seen 1x). If your change doesn't address it, expect the gate to fail again.
3. Changes here usually touch `page.tsx`, `layout.tsx`, `blind_tdd_audit.py`. Either inspect those files for shared state, or split the commit so a single boundary fails per gate run.
4. After applying any of the above, tag the commit message with `<lesson_applied stem='file-failure-vitest-config-ts-ka-site' how='N' />` where N is the step you used. This is how the corpus learns whether the lesson is actually working (see docs/bridge-protocol.md §4h).

