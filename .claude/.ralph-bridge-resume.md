# Bridge Pipeline Resume

<!-- Auto-injected by inject_lessons_interactive.py at SessionStart. -->
<!-- Bridge state survives sessions; read this BEFORE acting on tasks. -->

**Pipeline:** `623359fa` | **Phase:** `learn` | **Age:** 0.1 days (fresh)
**Tasks:** 62 done, 0 active, 0 pending

## Next Step

Extract patterns from this pipeline's observations and generate any new cross-project lessons:

```bash
$RALPH_HOME/ralph-learn.sh --dry-run   # preview
$RALPH_HOME/ralph-learn.sh             # extract + commit
python $RALPH_HOME/tools/bridge_state.py phase done
```

Re-derive any time with:
```bash
python $RALPH_HOME/tools/bridge_state.py what-now
```

