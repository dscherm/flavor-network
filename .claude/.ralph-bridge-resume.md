# Bridge Pipeline Resume

<!-- Auto-injected by inject_lessons_interactive.py at SessionStart. -->
<!-- Bridge state survives sessions; read this BEFORE acting on tasks. -->

**Pipeline:** `5aac001c` | **Phase:** `plan` | **Age:** 0.3 days (fresh)
**Tasks:** 4 done, 0 active, 44 pending

## Pending Queue

- **`R4-22`** — M1 RF baseline on Morgan fingerprints
  Then: `R4-23`, `R4-24`, `R4-25`, `R5-26`, `R5-27`, `R5-28`, `R5-29` …

## Next Step

Plan is loaded. Activate the next task to begin the task loop:

```bash
python $RALPH_HOME/tools/bridge_state.py activate R4-22
```

Re-derive any time with:
```bash
python $RALPH_HOME/tools/bridge_state.py what-now
```

## Degradation Signals

- **4 task(s) marked `done` with no gate_result and no observation_id** — looks like hand-edits that bypassed the bridge: `R3-18`, `R3-19`, `R3-20`, `R3-21`
- **Stats mismatch:** 4 task(s) marked done but `stats.tasks_completed = 0` — bridge_state.py was not the writer.

Run `python $RALPH_HOME/tools/bridge_state.py status` to inspect, and consider re-running the gate on suspect tasks before continuing.

