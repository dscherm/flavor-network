<!--
Lesson template — copy this file to lessons/<stem>.md, fill in, and stage
alongside your code commit. The agent IS expected to extend the lesson
corpus when it steps on undocumented traps; .claude/.ralph-lessons.md is
read+extend, not read-only.

Stem naming convention: kebab-case, project- or pattern-scoped. Examples:
  lessons/file-failure-app-py-murder-mystery.md
  lessons/error-importerror-dll-load-failed-while-impor.md
  lessons/repetition-drawing-agent-run-the-15-prompt-mvp-eval-end-to-e.md
-->
---
title: <one-line title that names the trap, not the fix>
severity: low | medium | high
tags: [<stack-tag>, <pattern-tag>, ...]
source: hand-authored
created: <YYYY-MM-DD>
project: <originating project name, optional>
---

## Symptom

What the failure looks like in practice — error signature, observable
behavior, the gate output that surfaced it. Concrete enough that a
future agent reading this can pattern-match within seconds.

## Root cause

Why it happens. The structural reason, not just the surface fix. If you
don't know the root cause, say so explicitly and mark severity accordingly.

## Mitigation

Numbered steps that actually fix or avoid the failure. The 1-indexed
numbers here are what `<lesson_applied stem='<this-file>' how='N' />`
references. Keep them concrete; an agent has to be able to *do* step N
without further interpretation.

1. ...
2. ...
3. ...

## Notes (optional)

Edge cases, related lessons, when this rule does NOT apply. The
counterfactual matters: if the mitigation has known false-positive
shapes, document them so future agents don't over-apply.
