{"task": 1, "category": "critical", "description": "Add gate failure feedback loop — write gate output to .ralph/gate_failure.md on failure, clear on success, update PROMPT.md to instruct agent to fix gate failures first", "steps": ["Create .ralph/ directory with gate_failure.md (empty)", "In ralph.sh, after gate runs: on failure write output to .ralph/gate_failure.md, on success clear it", "Update PROMPT.md step 1 (Orient) to check .ralph/gate_failure.md first — if non-empty, fix that before picking a new task", "Test: manually create a gate_failure.md and verify PROMPT references it"], "passes": false}
{"task": 2, "category": "critical", "description": "Add per-iteration timeout (default 1800s) — kill agent process if it exceeds timeout, treat as gate failure", "steps": ["Add --timeout flag to ralph.sh (default 1800)", "Wrap claude invocation with timeout command: timeout $TIMEOUT claude ... || handle", "On timeout: write 'TIMEOUT: agent exceeded N seconds' to .ralph/gate_failure.md", "Log timeout in iteration output"], "passes": false}
{"task": 3, "category": "critical", "description": "Add startup safety checks — dirty tree abort, main/master branch warning, stale lock detection improvement", "steps": ["After lock check in ralph.sh, add: git status --porcelain check (abort if modified/staged files, ignoring untracked)", "Add: branch check — if current branch is main or master, print warning and require --force to continue", "Add: consecutive failure counter (initialize to 0, increment on gate fail, reset on pass, warn at 3)", "Test with dirty tree and main branch"], "passes": false}
{"task": 4, "category": "high", "description": "Add secrets scan and staged-file denylist to gates.sh — hard block on leaked credentials or sensitive files", "steps": ["In gates.sh, add Gate 5 (Secrets scan): grep staged diff for AWS_SECRET, PRIVATE_KEY, api_key=, password=, Bearer token patterns", "Add Gate 6 (Denylist): check staged files against patterns .env*, *.pem, *.key, credentials.*, id_rsa, id_ed25519", "Both gates are BLOCKING (fail the gate, prevent commit)", "Test: stage a file with 'api_key=abc123' and verify gate fails"], "passes": false}
{"task": 5, "category": "high", "description": "Create activity.md with structured entry format — template header, current status block, entry format spec", "steps": ["Create activity.md at project root with header template", "Format: ## YYYY-MM-DD - TASK-N: Title / Goal / Changes Made / Verification / Status", "Add current-status block: last updated, tasks completed, test suite status", "Update PROMPT.md step 5 (Record) to instruct agent to append activity.md entries", "Update mini-ralph prompts step 5 similarly"], "passes": false}
{"task": 6, "category": "high", "description": "Add metrics.jsonl per-iteration tracking — append one JSON line per iteration with duration, task, gate result, files changed", "steps": ["In ralph.sh, record iteration start time before claude invocation", "After iteration completes, compute duration", "Count files changed: git diff --name-only HEAD~1 2>/dev/null | wc -l", "Append JSON line to .ralph/metrics.jsonl: {iteration, preset, duration_s, gate_result, files_changed, timestamp}", "Create .ralph/metrics.jsonl if not exists"], "passes": false}
{"task": 7, "category": "high", "description": "Create PLAN_PROMPT.md for read-only codebase analysis mode — discovers issues, generates prioritized fix_plan", "steps": ["Create PLAN_PROMPT.md: read-only analysis prompt that scans for TODOs, placeholders, spec drift, test gaps, dead code", "Instructs agent to run test suite and type checker", "Output format: fix_plan.md with CRITICAL/HIGH/MEDIUM/LOW tiers", "Signal: <promise>PLAN_COMPLETE</promise>", "Add --plan flag to ralph.sh that uses PLAN_PROMPT.md instead of PROMPT.md (single iteration, no loop)"], "passes": false}
{"task": 8, "category": "medium", "description": "Add context preparation — create prepare_context script that extracts pending tasks and recent activity into slim files", "steps": ["Create tools/prepare_context.cjs (Node.js, stdlib only)", "Reads fix_plan.md → extracts only pending tasks → writes .ralph/pending_tasks.md", "Reads activity.md → extracts last 5 entries → writes .ralph/recent_activity.md", "In ralph.sh, run prepare_context before each iteration", "Update PROMPT.md to reference slim files instead of full fix_plan.md"], "passes": false}
{"task": 9, "category": "medium", "description": "Add consecutive gate failure detection — track failures, warn at threshold, auto-BLOCKED at repeated identical failures", "steps": ["In ralph.sh, add CONSECUTIVE_FAILURES counter (init 0)", "On gate fail: increment counter, check if gate output identical to last failure (same-error detection)", "At threshold (3): print prominent warning", "If same error 3 times: auto-emit BLOCKED signal", "On gate pass: reset counter to 0"], "passes": false}
{"task": 10, "category": "medium", "description": "Add ralph_status dashboard script — show iterations, tasks done/remaining, avg duration, gate pass rate, estimated cost", "steps": ["Create tools/ralph_status.cjs (Node.js, stdlib only)", "Reads .ralph/metrics.jsonl → computes stats", "Reads all *-ralph/plan.md → counts tasks done/remaining", "Output: iterations total, tasks done/remaining per loop, avg duration, gate pass rate, anomalies (>2x avg duration, 0 files changed)", "Add --oneline flag for compact output", "Wire into ralph.sh --status as enhanced dashboard"], "passes": false}
{"task": 11, "category": "low", "description": "Create ralph.ps1 PowerShell variant — identical logic to ralph.sh for native Windows support", "steps": ["Translate ralph.sh to PowerShell syntax", "Same flags: --preset, --max, --dry, --status, --list-presets, --chain, --timeout, --plan", "Same lock file, signal detection, gate execution", "Test on PowerShell"], "passes": false}
{"task": 12, "category": "low", "description": "Document determinism boundaries — add section to mini-ralph-reference.md about non-determinism, reproducibility, iteration logs as audit trail", "steps": ["Add ## Determinism and Reproducibility section to .claude/docs/mini-ralph-reference.md", "Document: task-level roughly reproducible, sequence-level not reproducible", "Iteration logs (.ralph/logs/) are the replay mechanism", "Metrics.jsonl for trend analysis", "Recommend pinning model ID"], "passes": false}
