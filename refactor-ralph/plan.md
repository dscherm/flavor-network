{"task": 1, "category": "high", "description": "Extract utility functions (easeInOutCubic, hashStr, seededRng, makeLabel, computeWheelPositions, ingredientHasTaste) from LivingArchView lines 25-156 into src/components/livingArchUtils.js", "steps": ["Create livingArchUtils.js with all 6 functions exported", "Replace inline definitions in LivingArchView with imports", "Run npm run build to verify"], "passes": false}
{"task": 2, "category": "high", "description": "Extract constants (TASTE_ORDER, TASTE_HEX, CATEGORY_RADII, animation durations) from LivingArchView lines 10-23 into src/components/livingArchConstants.js", "steps": ["Create livingArchConstants.js with named exports", "Replace inline constants in LivingArchView with imports", "Run npm run build"], "passes": false}
{"task": 3, "category": "high", "description": "Move inline edge and particle shader code from LivingArchView (lines 337-418) into ShaderMaterials.js as exported factory functions", "steps": ["Add createLivingEdgeMaterial() and createLivingParticleMaterial() to ShaderMaterials.js with uBrightness uniform", "Replace inline ShaderMaterial construction in LivingArchView with calls to these factories", "Verify edge and particle rendering unchanged", "Run npm run build"], "passes": false}
{"task": 4, "category": "high", "description": "Extract taste selection state machine (lines 522-689) into custom hook useTasteSelection.js — handles taste1/taste2 selection, pop-out animation state, Y-offset computation", "steps": ["Create src/hooks/useTasteSelection.js exporting the taste selection logic", "Hook should accept nodeArray, validEdges, defaultColors, and return state + handlers (handleTasteClick, clearTasteSelection, tasteSelection object)", "Replace inline taste logic in LivingArchView with hook usage", "Verify taste click → pop-out animation still works", "Run npm run build"], "passes": false}
{"task": 5, "category": "high", "description": "Break the 236-line animate() function (lines 779-1014) into named sub-functions: updateModeTransition(), updateTasteAnimation(), updateParticlePositions()", "steps": ["Extract mode transition lerp logic into updateModeTransition()", "Extract taste pop-out animation into updateTasteAnimation()", "Extract particle position updates into updateParticlePositions()", "Keep animate() as thin orchestrator calling these 3 functions", "Verify all animation behaviors identical", "Run npm run build"], "passes": false}
{"task": 6, "category": "medium", "description": "Consolidate the 5 color-management useEffects (selection, edge visibility, particle visibility, taste filter, tree filter, bridge path — lines 1061-1246) into cleaner grouped effects", "steps": ["Group related effects: visibility toggles into one effect, color/highlight management into another", "Ensure dependency arrays are correct — no missing or extra deps", "Verify selection highlighting, taste filtering, tree filtering, bridge path all work", "Run npm run build"], "passes": false}
{"task": 7, "category": "medium", "description": "Extract raycasting and mouse handler functions (lines 691-752) into a useRaycast helper or inline named functions outside the setup useEffect", "steps": ["Move raycastLabels, onClick, onMove into clearly named functions", "Ensure they still have access to stateRef and scene objects", "Verify node clicking and hover cursor still work", "Run npm run build"], "passes": false}
{"task": 8, "category": "low", "description": "Final verification — confirm LivingArchView is under 500 lines, all features work, no regressions", "steps": ["Count lines in LivingArchView.jsx", "Verify: 3D neural mode renders correctly", "Verify: 2D wheel mode renders and taste labels clickable", "Verify: taste pop-out animation works (single + dual taste)", "Verify: edge/particle brightness sliders work", "Verify: node selection highlights neighbors", "Verify: tree filter and bridge path highlighting work", "Run npm run build"], "passes": false}
