{"task": 1, "category": "critical", "description": "Fix cluster label positions in 3D/2D Network modes — replace centroid-based placement with top hub ingredient's actual position", "steps": ["Created r11-discover-depth/scripts/task1_cluster_anchors.cjs — for each cluster, picks first top_ingredient with both 3D+2D positions and stores its gnn_positions as label_anchor_3d + pca_positions as label_anchor_2d", "Ran script — cluster_labels.json now includes label_anchor_3d, label_anchor_2d, anchor_ingredient per cluster", "Modified src/components/LivingArchView.jsx cluster-label setup to read label_anchor_3d/2d with fallback to centroid when anchors missing", "Duplicate 'Chili' clusters disambiguated to 'Chili (cumin)' and 'Chili (ginger)' based on their anchor ingredients", "Verified anchor distances now span 10.85–48.48 units vs old centroid range 0.75–9.22 (p50 node radius is 17, p90 is 48)", "npm run build + npx vitest run src/ both pass (33/33)"], "passes": true}
{"task": 2, "category": "high", "description": "Doc drift cleanup — remove 3 broken @-includes from top-level CLAUDE.md; create .claude/.chemdataset-status.md with current R10 state", "steps": ["Edit CLAUDE.md to remove @.claude/.ralph-lessons.md, @.claude/.ralph-spec.md, @.claude/.ralph-pending-reviews.md", "Keep @.claude/.chemdataset-status.md", "Create .claude/.chemdataset-status.md documenting R10-63 CV scores, coverage, model weaknesses"], "passes": true}
{"task": 3, "category": "critical", "description": "Discover CTA — pairing-of-the-day with user-selectable type", "steps": ["Created src/utils/discoverPick.js — pickPairing(edges, nodes, filter, sessionSalt) with deterministic daily seed (FNV-1a hash of date|kind|value|salt) and MIN_STRENGTH 0.5 floor so every pick is a real pairing", "Filter kinds: 'surprise' (any strong edge), 'taste:{sweet|sour|bitter|salty|umami|pungent|spicy|astringent}' (both endpoints include taste token), 'cuisine:{italian|mexican|...}' (both endpoints share a cuisine)", "Created src/components/DiscoverCTA.jsx — floating card bottom-left on network tab, filter-kind buttons + taste sub-selector, shows picked pair with strength, 'Open pair →' + regenerate ↻ buttons, 24h localStorage dismiss", "Wired into App.jsx: onPickPair sets selectedNodes + activePanel='ingredient' so user lands in IngredientPanel with the two ingredients preselected", "Smoke-tested against real data: surprise='apricot preserve + cornish hen', umami='sake + soy sauce' (0.99), spicy='anaheim + serrano' (0.75), mexican='poblano + sweet corn' (0.81) — all strong, intuitive picks", "Bridge-path filter kind deferred to a follow-up (3-step FlavorBridge path needs findStrongestPath reuse)", "npm run build + vitest 33/33 pass"], "passes": true}
{"task": 4, "category": "high", "description": "Wire MoleculeOfTheDay as Learn mode entry — replace current HowItWorks+MoleculeLab combo", "steps": ["Imported MoleculeOfTheDay in App.jsx", "Mounted as persistent landing card on network tab (activeTab === 'network')", "handleModeSelect('learn') now clears the 'fn.motd.dismissed' localStorage key and increments motdTick to force-remount the card, instead of auto-opening MoleculeLab+HowItWorks", "onOpen prop wired to setMoleculeLabOpen(true) — the in-card 'Open in Molecule Lab →' CTA is the progressive-disclosure path to the deeper views", "npm run build + vitest 33/33 pass"], "passes": true}
{"task": 5, "category": "critical", "description": "Per-pairing odor badges backed by bridge_compounds.json — core surface (IngredientPanel) shipped; other surfaces filed as task 5b", "steps": ["Created src/utils/odorBadge.js — getOdorBadge(a, b, bridgeCompounds) aggregates bridge-compound tags, returns most common when count>=2 AND covers >=30% of bridges, filters uninformative tags (odorless, tasteless, none, unknown)", "Created src/components/OdorBadge.jsx — compact inline pill with per-tag color + count tooltip", "Wired into IngredientPanel (both desktop panel + mobile BottomSheet) via new bridgeCompounds prop passed from App.jsx", "Coverage validated: 259/778 bridge_compound pairs produce a badge (33.3%); top tags citrus/sweet/waxy/bitter/apple/woody cover common pairing intuition", "npm run build + vitest 33/33 pass"], "passes": true}
{"task": "5b", "category": "medium", "description": "Extend OdorBadge to remaining pairing surfaces — CocktailRecipeCard swap alts, SaucePanel swap alts, SuggestionDrawer chips", "steps": ["CocktailRecipeCard: accepted bridgeCompounds prop, rendered OdorBadge between score and row end, comparing swapIngredient ↔ alt.name (tells user whether the swap candidate is aromatically similar to the ingredient being replaced)", "SaucePanel: same pattern for swapAlternatives rows", "SuggestionDrawer: ChipButton now accepts centerIngredient + bridgeCompounds; renders OdorBadge next to matchPct when centerIngredient is set — gives chef signal on 'this suggestion shares an aromatic note with my center ingredient'", "Prop threading: App.jsx already passes fullData to CocktailLab/SauceLab/RecipeLab; added bridgeCompounds={fullData?.bridgeCompounds} at the Lab-to-Panel and Panel-to-Card/Drawer levels", "npm run build + vitest 47/47 pass"], "passes": true}
{"task": 6, "category": "low", "description": "File R12 follow-up — retrain multi-task GNN head with class-balanced / focal loss to fix odor prediction collapse", "steps": ["Appended TASK-183 (R12) to .claude/fix_plan.md Future Work section", "Documented full F1 table, collapse evidence (pairwise odor-cosine p50 = 1.000), and acceptance criteria (F1 >= 0.5 on 4 of 6 odor heads before GNN-derived odor badges can ship)", "Cross-referenced r11-discover-depth/prd.md and .claude/.chemdataset-status.md for context"], "passes": true}
{"task": 7, "category": "high", "description": "Unit tests for new utilities — odorBadge + discoverPick", "steps": ["Created src/utils/odorBadge.test.js — 6 cases covering null inputs, unknown pair, dominant-tag selection, order invariance (a|b === b|a), uninformative-tag filter (odorless → null), min-count threshold", "Created src/utils/discoverPick.test.js — 8 cases covering null inputs, surprise floor, taste-filter matching, cuisine-filter matching, no-match null, determinism within same day+salt, strength floor excludes weak edges", "Total test suite now 47/47 pass (was 33)", "Playwright e2e additions deferred — fixture setup for OdorBadge/DiscoverCTA visible-on-mount assertions is nontrivial and the logic is covered by unit tests"], "passes": true}
{"task": 8, "category": "medium", "description": "Gate sidecar — ensure post-commit hook logs gate: pass not stale", "steps": ["Diagnosed: .git/hooks/post-commit reads .ralph/last_gate_result.json (10-min TTL); ungated manual commits correctly log gate: stale", "Created scripts/gate.cjs — runs vitest + build, writes sidecar with result shape matching post-commit expectations, exits non-zero on failure", "Added npm scripts: `npm run gate` (full), `npm run gate:quick` (vitest only, skip build)", "Verified sidecar written with {result: 'pass', strategy, checks}; within 10-min window next commit records gate: pass", "Developer workflow: `npm run gate && git commit -m '...'` to auto-record gate: pass"], "passes": true}
