name: iOS Build

on:
  push:
    branches: [master]
  workflow_dispatch: # Allow manual trigger

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build web app
        run: npm run build

      - name: Sync Capacitor iOS
        run: npx cap sync ios

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Build iOS (unsigned for CI validation)
        working-directory: ios/App
        run: |
          xcodebuild build \
            -workspace App.xcworkspace \
            -scheme App \
            -configuration Debug \
            -destination 'generic/platform=iOS Simulator' \
            -allowProvisioningUpdates \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            | tail -20

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ios-build
          path: ios/App/build/
          retention-days: 7

  # Uncomment this job when you have Apple Developer credentials set up
  # build-and-deploy:
  #   runs-on: macos-latest
  #   needs: build-ios
  #   if: github.ref == 'refs/heads/master'
  #   steps:
  #     - name: Build for App Store
  #       working-directory: ios/App
  #       run: |
  #         xcodebuild archive \
  #           -workspace App.xcworkspace \
  #           -scheme App \
  #           -configuration Release \
  #           -archivePath $RUNNER_TEMP/App.xcarchive \
  #           CODE_SIGN_IDENTITY="${{ secrets.APPLE_CERT_IDENTITY }}" \
  #           PROVISIONING_PROFILE_SPECIFIER="${{ secrets.APPLE_PROVISIONING_PROFILE }}"
  #
  #     - name: Export IPA
  #       run: |
  #         xcodebuild -exportArchive \
  #           -archivePath $RUNNER_TEMP/App.xcarchive \
  #           -exportPath $RUNNER_TEMP/export \
  #           -exportOptionsPlist ios/ExportOptions.plist
  #
  #     - name: Upload to App Store Connect
  #       run: |
  #         xcrun altool --upload-app \
  #           -f $RUNNER_TEMP/export/App.ipa \
  #           -u ${{ secrets.APPLE_ID }} \
  #           -p ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
